 void Start()
    {
        // Set first list to first video meshes and textures
        // Set second list to second video meshes and textures
        // Add video player component
        // Pass first list to video player to start playing
    }

    void Update()
    {
        // Check if video player played first list
        // When it does set list = second list and second list = next list
        // When second list reaches end set second list to very firt list
    }
